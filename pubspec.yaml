name: notes_rag
description: "Marginal — Fully on-device RAG notes app"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter
  flutter_gemma: ^1.8.0
  flutter_gemma_litertlm: ^1.6.3
  flutter_gemma_rag_sqlite: ^1.3.1
  flutter_riverpod: ^2.6.1
  go_router: ^14.8.1
  google_fonts: ^6.2.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  syncfusion_flutter_pdf: ^30.2.7
  path_provider: ^2.1.5
  path: ^1.9.1
  uuid: ^4.5.1
  file_picker: ^12.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
